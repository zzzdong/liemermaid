/target

*.svg
*.png
*.html
# 但 golden 测试基线（官方 + 自比）必须纳入版本控制，供 CI 回归比对
!tests/golden/**/*.svg
# site 演示页必须纳入版本控制（GitHub Pages 工作流会拷贝 site/index.html）
!site/index.html
# wasm-pack 本地产物不入库（CI 构建到 _site 时会重新生成）
/site/pkg

**/node_modules
