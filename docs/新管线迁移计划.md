# 新管线迁移计划与进度跟进

> 目标：以 flowchart 为代表的**新四阶段管线**为主，把全部图表类型逐步切换过去，最终删除旧管线。
> 本文档是改造计划的唯一事实来源，后续所有修复/迁移均在此更新进度。

## 1. 两条管线架构

### 新管线（目标形态）

```
AST → extract → Unigraph(UG) → measure → UG' → engine → (Geograph + StyleIntent)
        → materialize → SceneGraph → paint → lievisual::Scene
```

- **extract**（`src/builder/extract/`）：AST → 统一拓扑图 `Unigraph`，不碰坐标/尺寸/颜色。
- **measure**（`src/builder/measure/`）：测量文本尺寸写回 UG。
- **engine**（`src/builder/layout/engine.rs`）：分层 → 坐标分配 → 端口解析 → 边路由 → 容器包围盒。
- **materialize**（`src/builder/materialize/`）：唯一消费 theme 常量的视觉决策点，产出 `SceneGraph`。
- **paint**（`src/builder/paint/`）：零分支纯翻译 `SceneGraph` → `lievisual::Scene`。

分派入口：`src/builder/mod.rs::build_diagram_with_config`，`extract::run` 成功则走新管线，失败则降级旧管线。

### 旧管线（待淘汰）

```
AST → layout_diagram(LayoutGraph) → solver → PlacedGraph → render → SceneNode
```

- `src/builder/layout/mod.rs::layout_diagram`（入口）
- `src/builder/render/mod.rs::render_placed`（渲染分派）

### 当前覆盖范围

| 图表类型 | Family | 现状 |
|---|---|---|
| flowchart | Directed | ✅ 新管线（默认 Spline 路由） |
| state | Directed | ✅ 新管线（默认 Orthogonal 路由） |
| class | Grid | ✅ 新管线（已完整迁移） |
| er | Grid | ✅ 新管线（已完整迁移） |
| sequence | Sequence | ✅ 新管线（已完整迁移） |
| timeline | Linear | ✅ 新管线（已完整迁移，删旧渲染） |
| pie | Radial | ✅ 新管线（已完整迁移，删旧渲染） |
| gitgraph | Hierarchy | ✅ 新管线（已完整迁移） |

---

## 2. 当前进度总览

| 项 | 状态 | 说明 |
|---|---|---|
| 阶段 0.1 碰撞原语共线假阳性 | ✅ 完成 | `spatial.rs` 重写 `segment_intersects_segment` + 3 个回归用例 |
| 阶段 0.2 回边补碰撞检测 | ✅ 完成 | 所有回边统一走 `plan_back_channel`，修复 state 的 Orthogonal 回边穿节点 |
| 修复 state 复合状态边重写 | ✅ 完成 | `extract/state.rs`：source→出口、target→入口（原方向颠倒致 start/final 入容器） |
| 阶段 1 class/er 迁移 | ✅ 完成 | 见 §4 |
| 阶段 0.3 容器避障 | ✅ 完成 | `route_edges` 感知 subgraph 容器框（`GGContainer.member_ids` 成员语义 + `Obstacles` 统一避让） |
| 阶段 0.4 SpatialGrid 去留 | ✅ 完成 | 删除 `SpatialGrid`（死代码），保留 `segment_intersects_rect` 等原语 |
| 阶段 0.5 布局常量收敛 | ✅ 完成 | 删 `layout/config.rs` 后 `engine.rs` 的 `LAYER_GAP`/`NODE_GAP`/`EDGE_LABEL_GAP` 为唯一源 |
| 阶段 0.6 正交边-边避让 | ✅ 完成 | 正交主干互斥（`used_trunks`），并行边侧向错开；节点避让优先于边间避让 |
| 阶段 2 timeline | ✅ 完成 | 端到端迁移（extract→measure→engine Linear→materialize→接入，删 `render/timeline.rs`） |
| 阶段 2 sequence | ✅ 完成 | 端到端迁移（engine Sequence + materialize + 接入） |
| 阶段 3 pie | ✅ 完成 | 端到端迁移（Radial 家族 + `emit_pie` + 接入） |
| 阶段 3 gitgraph | ✅ 完成 | 端到端迁移（Hierarchy 家族 + `emit_gitgraph` + 接入） |
| 阶段 4 删除旧管线 | ✅ 完成 | 见 §7（solver/render/convert/ir 等全部删除，降级分支移除，旧测试清理） |
| 阶段 5.1 视觉常量对齐 | ✅ 完成 | 见 §13.1（箭头实心 / 线宽 / sequence 配色 / pie 常量） |
| 阶段 5.2 节点尺寸自适应 | ✅ 完成 | 见 §13.2（官方 padding 表 + `line-height:1.5`，去掉 120×60 下限） |
| 阶段 5.3 布局间距对齐 | ✅ 完成 | 见 §13.3（rankSpacing/nodeSpacing = 50；sequence 行距 46） |
| 阶段 5.4 pie 图例 + 外圈 | ✅ 完成 | 见 §13.4 |
| 阶段 5.5 文本格式对齐 | ✅ 完成 | 见 §13.5（class 成员 / sequence 块标签） |
| 阶段 5.6 sequence 激活条 | ✅ 完成 | 见 §13.6（`->>+` / `-->>-`，含"取消激活作用于源"的语义修正） |

---

## 3. 阶段 0 —— 新管线加固

> 目的：在切换前把新管线本身的正确性补强，避免把缺陷带进后续图表。

- [x] **0.1 碰撞原语共线假阳性** — 完成。`spatial.rs` 的 `segment_intersects_segment` 重写，`route.rs` 依赖它做边-节点避让。
- [x] **0.2 回边补碰撞检测** — 完成。`BackBow` 与 Orthogonal 回边此前不检测穿节点，统一先走 `plan_back_channel` 通道绕行。实际修复的是 **state** 图（默认 Orthogonal），flowchart（默认 Spline）本就走通道，行为无变化。
- [x] **0.3 容器避障** — 完成。`GGContainer` 新增 `member_ids`；`engine::run` 先在路由前计算容器，`route_edges(gg, direction, &containers)` 引入 `Obstacles`（节点矩形 + 容器矩形 + 成员映射），用 `edge_skip` 闭包统一「端点节点 / 容器成员」跳过逻辑。规则：边只绕开**两端均非其成员**的容器，容器内边 / 跨容器边（至少一端为成员）允许穿过边界。覆盖 Spline 绕行通道、Orthogonal 主干、回边 `plan_back_channel` 通道三处。新增单测 2 个（`normal_edge_avoids_container` / `intra_container_edge_stays_inside`）+ 集成回归 `edges_avoid_non_member_containers`（flowchart subgraph / state composite）。
- [x] **0.4 SpatialGrid 去留** — 完成。决策：**删除**。`SpatialGrid` 仅被自身测试引用，`route.rs` 只用 `segment_intersects_rect`/`segment_intersects_segment`；删除网格哈希结构与对应测试，保留碰撞原语。
- [x] **0.5 布局常量收敛** — 完成。`layout/config.rs` 已随阶段 4 删除，`engine.rs` 的 `LAYER_GAP=56` / `NODE_GAP=48` / `EDGE_LABEL_GAP=28` 成为唯一来源，不再有常量双源。
- [x] **0.6 正交边-边避让** — 完成。`orthogonal_route` 新增 `used_trunks: Vec<(trunk, s0, s1)>` 登记已占用正交主干；候选主干评分字典序 (节点冲突数, 是否占用主干, |偏移|)，节点避让优先于边间避让（与 Spline 通道两轮评估一致）。回归用例 `orthogonal_edges_do_not_share_trunk`（交叉对 A1→B1 / A2→B2 主干 x=0 冲突 → 侧向错开到 0 / 12）。

---

## 4. 阶段 1 —— class / er 迁移（Grid 家族）✅ 已完成

每步独立可验证、不破坏 flowchart/state。四步已全部落地：

1. **IR 扩展 + extract**
   - `ir/common.rs` 新增 `NodeDetail`（`Class { annotation, attrs, methods }` / `Entity { attrs }` / `None`）。
   - `extract/class.rs::extract_class`、`extract/er.rs::extract_er`，`family = GraphFamily::Grid`。
2. **measure + engine Grid 布局**
   - `measure/mod.rs::measure_structured_label`：类框三栏 / 实体框两栏尺寸测量。
   - `engine.rs::grid_layers`：Grid 家族的 BFS 分层（入度 0 为根，层内保持源码序）。
3. **materialize**
   - `emit_class_box`（三栏：header + attrs + methods）、`emit_entity_box`（两栏）。
   - `emit_cardinality`（ER 基数 `||`/`|o`/`}|`/`}o`）、`edge_ends`（class 关系三角/菱形端点）。
4. **paint + 接入**
   - `paint/mod.rs::arrow_element` 补特殊端点：`Triangle`/`TriangleFilled`/`DiamondFilled`/`DiamondHollow`/`Cross`/`Circle`。
   - `extract/mod.rs` 已接入 class/er，`build_diagram` 因此对 class/er 走新管线。

> class/er 旧渲染（`render/class.rs`、`render/er.rs`、`solver/grid.rs`）已不再被走到，属阶段 4 删除范围。

---

## 5. 阶段 2 —— sequence / timeline（Sequence / Linear 家族）

### 5.1 timeline ✅ 已完整迁移（删旧渲染）

- **IR**：`Geograph` 新增 `title`；`NodeDetail::TimelineSection { events }`；`measure` 按 `BLOCK_W` 下限给出列包围盒。
- **engine**：`run()` 按 `GraphFamily::Linear` 分支 → `linear_centers`（LR：各列同一水平线 y=0，x 递增；TD：各列同一垂直线，y 递增）。
- **materialize**：`emit_timeline` 检测到 TimelineSection 节点即整体绘制——标题、带箭头主轴线、时间点（实心圆）、section 彩色块（轴上方）、事件彩色块（轴下方）、虚线连接线 + 箭头。
- **接入**：`extract::run` 对 `Diagram::Timeline` 返回 `Ok`，timeline 走新管线；删除 `render/timeline.rs` 及其 `LinearRenderer` 分派分支。
- 测试：engine `linear_lr_lays_out_sections_in_row` / `linear_td_lays_out_sections_in_column`；端到端 `timeline_renders_sections_and_events` / `timeline_without_title_renders`。

### 5.2 sequence ✅ 已完整迁移

> 旧管线由 `LinearSolver` + `render/sequence.rs` 承担（生命线/激活条/消息时序几何）。

- **IR**：
  - `Unigraph` 新增 `sequence_rows: Option<Vec<SequenceRow>>`——语句行（`Message(EdgeId)` / `Note(NodeId)` / `BlockStart(id, label)` / `BlockEnd(id)`），按源码序保留纵向行序与块嵌套（Sequence 是线性语句序列而非普通图，这是行序的核心载体）。
  - `NodeDetail::SequenceNote { text, targets, placement }` + `SequenceNotePlacement`（Over/LeftOf/RightOf）；`ContainerKind::SequenceBlock`；`NodeRole::Lifeline`。
- **extract**：`extract_sequence` 产出参与者节点 + `sequence_rows`；消息 → `SequenceMessage` 边（标签 / 箭头 / 线型），备注 → `SequenceNote` 节点，分组块 → `BlockStart/End` 包裹块内语句。
- **engine**：`run()` 按 `GraphFamily::Sequence` 分支 → `sequence_geometry`：参与者按列宽排 x（生命线）、语句按行推进 y、消息水平路由（自环右侧小环）、备注据放置位置定框、分组块据成员行范围生成容器。
- **materialize**：`emit_sequence` 整体绘制——参与者盒 + 虚线生命线、消息水平线 + 箭头 + 标签、备注框、分组块虚线边框 + 标签。
- **接入**：`extract::run` 对 `Diagram::Sequence` 返回 `Ok`；`render/sequence.rs` 的 mod 声明已移除（文件待阶段 4 物理删除）。
- 测试：端到端 `sequence_renders_participants_messages_and_notes` / `sequence_renders_loop_block_label`；extract 单测（行序 / 块嵌套 / 备注）。
- 已知取舍：激活条（`->>+B` / `-->>-A`）暂未渲染（IR 未记录激活标记），后续在 IR 加 `SequenceActivation` 即可补。

---

## 6. 阶段 3 —— pie / gitgraph

### 6.1 pie ✅ 已完整迁移（Radial 家族）

- **IR**：`NodeDetail::PieSlice { label, value }`（扇区数据）；`DiagramMeta.show_data`；`Geograph.show_data`。
- **extract**：`extract_pie`（family = Radial），每数据项一个节点，值解析失败返回错误。
- **engine**：Radial 分支把所有节点叠于原点（扇区角度由 materialize 从数据计算）。
- **materialize**：`emit_pie`——从 12 点方向顺时针画扇区（`ShapeGeometry::Pie`，paint 直传 `Element::pie`）、白描边分割、扇形外缘百分比标签（`showData` 时附数值）、标题。
- **接入**：`extract::run` 对 `Diagram::Pie` 返回 `Ok`；`render/pie.rs` 的 mod 声明已移除。
- 测试：extract 单测（扇区/数值/非法值报错）；既有 `pipeline_test` 的 pie 结构断言（3 扇区 = 3 `<path>`、百分比、showData 数值）全部保持通过。

### 6.2 gitgraph ✅ 已完整迁移（Hierarchy 家族）

- **IR**：`NodeDetail::GitCommit { branch, tag, is_merge }`（提交点）；`ContainerKind::GitBranch`（分支容器，member_ids = 该分支提交，顺序 = 分支首见序）。
- **extract**：`extract_gitgraph`（family = Hierarchy）——每个提交（commit / merge / cherry-pick）一个节点，父引用为 `Generic` 边（child → parent），分支为子图容器；merge 双亲边、默认标签 `merge <branch>`。
- **engine**：`hierarchy_geometry`——提交按声明序沿 x 推进（`COMMIT_SPACING`）、分支按容器序映射到 y 行（`BRANCH_SPACING`）；边路由留空。
- **materialize**：`emit_gitgraph`——提交点圆（merge 空心白底彩边）、同分支水平线、跨分支垂直 + quad 曲线（父分支色）、tag 标签、分支彩色标签块 + 白字 + 尾部延伸线。
- **接入**：`extract::run` 对 `Diagram::GitGraph` 返回 `Ok`；至此 **8/8 图类型全部新管线**，`extract::run` 的 match 已穷尽（无降级分支）。
- 测试：extract 单测（分支容器成员 / merge 双亲边 / merge 标记 / `gitGraph:` 头部不吞首条 commit）；端到端 `gitgraph_renders_commit_ids_branches_and_dashed_lanes`。
- **对齐官方 golden 的修复**：
  1. **commit id 标签**：普通提交在圆点下方显示 commit id（显式 `id:` 或自动 `c{n}`），有 `tag:` 时显示 tag；merge 无显式标签时不显示（对齐官方 `.commit-label`）。
  2. **fork / merge 曲线**：跨分支连接改为官方「直线 → 四分之一圆弧 → 直线」——fork（子分支在下方）= 垂直下→弧→水平进；merge（子在上方）= 水平→弧→垂直进；曲线用贝塞尔（`k = r·0.5523`）逼近圆弧。fork 用子分支色、merge 用父分支色（对齐官方箭头 class）。
  3. **分支虚线**：每行画贯穿内容区的虚线（`#333, dasharray 2`），标识分支平行轨道。
  4. **parser 修复**：`gitGraph:` 头部 `skip_ws_and_comments` 改 `inline_ws`，避免跨行吞掉首条 `commit` 语句（导致提交数少一）。

---

## 7. 阶段 4 —— 删除旧管线（文件清单）

### 保留（新管线核心）

| 文件 | 角色 |
|---|---|
| `layout/engine.rs` | 新管线布局入口 |
| `layout/directed.rs` | `sugiyama_layers`（被 engine 引用；其内部 `minimize_crossings` 走 `crossing.rs`） |
| `layout/route.rs` | `route_edges`（边路由，被 engine 引用） |
| `layout/spatial.rs` | 碰撞检测（`segment_intersects_rect` 被 route 引用；`SpatialGrid` 已随 0.4 删除） |
| `layout/crossing.rs` | `minimize_crossings`（被 directed.rs 引用） |

### 删除（旧管线专属）✅ 全部完成

| 文件/目录 | 说明 |
|---|---|
| `layout/solver/*` | `DirectedSolver`/`GridSolver`/`LinearSolver`/`SimpleSolver`/`GroupedDirected`/`solver_for` — 已删 |
| `layout/sugiyama.rs` | 旧 Sugiyama 布局 — 已删 |
| `layout/convert.rs` | AST → `LayoutGraph` — 已删 |
| `layout/ir.rs` | `LayoutGraph` / `PlacedGraph` — 已删 |
| `layout/measure.rs` | 旧管线测量 — 已删 |
| `layout/recognize.rs` | 旧管线节点识别 — 已删 |
| `layout/state_nodes.rs` | 旧 state 节点收集 — 已删 |
| `layout/analyze.rs` / `coord.rs` / `types.rs` / `config.rs` | 旧布局辅助 — 已删 |
| `render/*`（全部 10 文件） | 旧渲染层 — 已删 |
| `builder/mod.rs` 中的降级分支 | `Err(_) => layout_diagram + render_placed` 整段 — 已移除 |

> 执行记录：`layout/mod.rs` 精简为 `{crossing, directed, engine, route, spatial}`；`build_diagram_with_config` 直接走新管线；`builder/mod.rs` 移除 `pub mod render`。旧管线专属测试一并删除：`render_test.rs`、`grouped_layout_test.rs`、`placed_graph_test.rs`、`layout_quality_metrics.rs`、`layout_ir_test.rs`、`sugiyama_consistency_test.rs`、`graph_analysis_test.rs`。全量 `cargo test --workspace` 0 failed。

---

## 8. 关键决策与风险

1. **迁移顺序**：从 Grid 家族（class/er）切入，再 sequence/timeline，最后 pie/gitgraph。✅ 全部完成。
2. **降级兜底**：迁移期间 `extract::run` 对未迁移类型返回 `Err` 走旧管线，保证 golden 不破坏——安全网，已在阶段 4 全部迁移完成后移除。
3. **常量双源**（0.5）：已随阶段 4 统一（`engine.rs` 为唯一源）。
4. **回归保障**：每阶段落地后跑全量 `cargo test`，当前 0 failed。

---

## 9. 完成情况与后续

**迁移全部完成**：8/8 图类型走新管线，旧管线（solver / render / convert / ir / sugiyama 等）已整体删除，`build_diagram_with_config` 无降级分支，`cargo test --workspace` 0 failed。

**官方 golden 对齐**：见 §13（阶段 5），已按 `docs/svg_alignment_report.md` 与 `docs/svg_alignment_report-qwen.md` 两份报告的结论逐项收敛。

可选后续（非迁移必需）：
1. `src/vir.rs`（旧视觉 IR）现已无新管线引用，仍作为公开模块保留；如需彻底清理可评估删除。
2. §13.7 列出的已知偏差（timeline 结构、state fork/join 语义等）。

---

## 10. 新管线设计

### 10.1 三层 IR + 两层视觉图

数据在管线中经历五次形态变换，核心是"**拓扑 → 几何 → 视觉**"的逐步具象，每层严守一条纪律：

| 阶段 | 产物 | 类型 | 含坐标 | 含颜色 | 职责纪律 |
|---|---|---|---|---|---|
| extract | `Unigraph`(UG) | `ir/unigraph.rs` | ❌ | ❌ | AST 唯一出口，不碰坐标/尺寸/颜色 |
| measure | UG' | 同 UG | ❌ | ❌ | 仅做文本测量，`LabelSpec`→`MeasuredLabel` |
| engine | `Geograph`(GG) | `ir/geograph.rs` | ✅ | ❌ | 仅做几何：坐标/尺寸/路由已定，仍无颜色 |
| materialize | `SceneGraph` | `ir/scenegraph.rs` | ✅ | ✅ | 唯一消费 theme 常量的视觉决策点 |
| paint | `lievisual::Scene` | 外部库 | ✅ | ✅ | 零分支纯翻译，不接收 `&Diagram`/`&Theme` |

**关键设计**：`extract` 产出 UG 后可被 drop，GG 与 UG 解耦——`engine` 结束时把"几何 + 样式意图(`StyleIntent`)"分离，`materialize`/`paint` 不持有 UG 引用，结构上杜绝回查 AST。

### 10.2 图家族分派（`GraphFamily`）

UG 携带 `family` 字段，决定 `engine` 走哪套布局/路由策略：

| Family | 覆盖图 | 布局策略 | 状态 |
|---|---|---|---|
| `Directed` | flowchart / state | Sugiyama 分层 + barycenter + 正交/样条路由 | ✅ |
| `Grid` | class / er | BFS 网格分层 + 关系路由 | ✅ |
| `Linear` | mindmap / timeline | 线性排布 | ⏳ 预留 |
| `Sequence` | sequence | 泳道 + 消息时序 | ⏳ 预留 |
| `Radial` | pie / quadrant | 极坐标 | ⏳ 预留 |
| `Hierarchy` | gitgraph / gantt / sankey | 层级 / 时间轴 | ⏳ 预留 |

> `extract` 透传 `direction`（TB/TD/BT/LR/RL）；`engine` 据此旋转坐标轴，与 family 无关。

### 10.3 关键 IR 类型

- **`NodeDetail`**（`ir/common.rs`）：结构化节点详情。`None`=普通单栏节点；`Class { annotation, attrs, methods }`=UML 类框三栏；`Entity { attrs }`=ER 实体框两栏。
- **`EdgeKind`**：边语义类别（`Flow`/`StateTransition`/`ClassExtends`/`ClassComposition`/…），驱动 materialize 选线型/箭头表。
- **`RoutingHint`**：`Orthogonal`（曼哈顿折线）/ `Spline`（贝塞尔）。flowchart 默认 Spline，state 默认 Orthogonal。
- **`LineKind`**：`Solid`/`Dotted`/`Thick`/`Invisible`，来自箭头语法（`-->`/`-.->`/`==>`/`~~~`）。
- **`ContainerKind`**：`Subgraph`/`StateComposite`/`Lifeline`/`ClassBox`/`Slice`。
- **`RoutePath`**（`ir/geograph.rs`）：连续 `RouteSegment`（`Line` 或 `CubicBezier`）序列，供 materialize/paint 按段画 `<line>` 或 `<path d="M..C..">`。

---

## 11. 布局算法

### 11.1 Directed 分层（`layout/directed.rs`）

三步：**反馈边检测 → 最长路径松弛分层 → 交叉减少**。

1. **反馈边检测 `detect_back_edges`**：迭代 DFS 三色标记（0 未访问/1 在栈/2 已结束），指向"当前 DFS 栈中祖先"的边记为 back edge。显式栈模拟递归，避免大图栈溢出。
2. **最长路径松弛 `assign_layers`**：层号 = 沿非 back edge 的最大前驱层 + 1，跳过 back edge（避免环把层号推高）；层号上限 `n-1`，`guard` 迭代上限 `n+1` 兜底。
3. **交叉减少 `minimize_crossings`**（`layout/crossing.rs`）：barycenter（重心）启发式，偶数轮自上而下、奇数轮自下而上，共 4 轮；每轮**动态**重算邻居当前位置（非静态邻接表），同分按插入序 tie-break 保证确定性。
4. **方向反转**：`direction` 为 `BT`/`RL` 时整体 `reverse()` 层序。

### 11.2 Grid 分层（`engine.rs::grid_layers`）

class/er 用 BFS 分层替代 Sugiyama：入度 0 节点为第 0 层，逐层推进；**层内保持源码序**（class 图按声明顺序排布），不做交叉减少；全成环（无入度 0）退化为单层。

### 11.3 坐标分配（`engine.rs::run`）

- **双轴模型**：主轴 = 层推进方向（TB 垂直 / LR 水平）；同层轴 = 节点展开方向。
- **层厚**（cross）= 层内节点在主轴方向的尺寸最大值；**层长**（main）= 同层轴尺寸累加 + `NODE_GAP`。
- **层间标签预留**：相邻两层间存在带标签边时额外 `EDGE_LABEL_GAP`，避免标签压节点。
- **居中**：主轴起点 = `-total_main / 2`，整体居中；同层内节点按 `NODE_GAP` 间距居中展开。
- 端口解析 `resolve_ports` 由 center + size 算四向端口；再据 `ShapeKind` 做形状感知边界投影。

### 11.4 容器包围盒（`engine.rs::compute_containers`）

据 `subgraphs[].member_ids` 找到已布局成员节点，计算 min/max 包围盒，四周 `PAD` + 顶部 `TITLE_H` 留白生成容器框。**容器不参与分层**，仅事后"包住"成员节点。

---

## 12. 碰撞检测与避让算法

### 12.1 碰撞原语（`layout/spatial.rs`）

- **`segment_intersects_segment`**：标准跨立实验 + 共线重叠判定。严格异号叉积 = 内部相交；端点叉积为 0（共线/端点接触）时用 `point_on_segment`（含 bbox 容差）判真接触。**修正旧实现 `d1*d2<=0 && d3*d4<=0` 把共线但分离的两线段误判为相交的假阳性**（阶段 0.1）。
- **`segment_intersects_rect`**：端点在内，或与矩形四边任一相交。

### 12.2 路由总控（`layout/route.rs::route_edges`）

为每条边分类生成 `Plan`，再逐边路由：

| Plan | 触发条件 | 策略 |
|---|---|---|
| `SelfLoop` | source == target | 节点右侧外凸环（单段贝塞尔） |
| `Mutual` | u↔v 双向同时存在 | 小偏移 + 轻微外弓的平行弧线 |
| `BackBow` | 回边且无阻挡 | 侧向 U 形贝塞尔 |
| `BackChannel` | 回边且有阻挡 | 侧边通道绕行（圆角折线） |
| `Normal` | 其余 | Spline / Orthogonal |

### 12.3 端口选择与分散

- **`pick_port`**：优先沿布局主轴出线（TB→Top/Bottom，LR→Left/Right），当对端明显在侧方时（`SIDE_BIAS=2.5` 判据）切到侧面端口，避免同层横向相邻点被迫绕一大圈。
- **`rank_slots`**：同一节点同一端口的多条边，按对端在该端口轴向的投影排序 → 分配槽位 `(idx, total)`，保证 fan-out/fan-in 空间顺序一致、不打结。
- **`port_point_at`**：槽位沿节点边居中对称分散，`spacing` clamp 到 `[PORT_MIN_SPACING, span×0.4]` 且不贴角；Diamond/Circle 形状按边界投影（端口永远落在形状边界上，不悬空）。

### 12.4 Spline 路由（`spline_route` + `spline_route_safe`）

- 单段三次贝塞尔：控制点沿端口法向延伸 `arc`，并沿垂直流向偏置 1/4 形成扇形展开。
- **单调性约束**：`arc` 只取两端口净间距 `gap` 的一半且 `arc <= gap`，保证曲线在主轴单调，同一源的多条出边**互不交叉**（旧实现按 `max(|dx|,|dy|)` 取弧度会甩过目标行形成 S 形回绕）。
- **节点回避**：先按朴素贝塞尔 `sample_path`（32 等分）采样检测；若穿过非端点节点，退化为「stub + 侧边通道 + 圆角折线」绕行。

### 12.5 Orthogonal 路由（`orthogonal_route`）

stub + 主干曼哈顿折线。主干位置（垂直主轴=共享 x，水平主轴=共享 y）从两 stub 端点中点出发，沿主轴法向双向步进（`OFFSET_STEP=12`，最多 8 次），取**冲突段数最少、偏移最小**的落点，处理长边跨层（主干初始位置落在中间层节点内）的情况。

### 12.6 回边通道规划（`plan_back_channel` + `settle_channel`）

回边（source 在主轴下游）检测两中心间阻挡带（`BAND_PAD` 外扩）是否命中中间节点：

1. 命中 → `settle_channel` 从命中节点外侧逐个让开挡路节点（`CHANNEL_MARGIN=16`）。
2. `try_channel_side` 再避开**已占用通道**：同坐标且跨度重叠时沿侧向 `CH_STACK=28` 堆叠重试（最多 6 次）。
3. 左右两侧候选按 `(交叉者计数, 距中线距离)` 评分选优，登记占用 `(ch, top, bot)`。
4. 无阻挡 → `BackBow` 侧向 U 形贝塞尔（本就安全）。

### 12.7 边间避让（`used_channels` + `used_trunks`）

- **绕行通道**：全局 `used_channels: Vec<(ch, top, bot)>` 登记已占用绕行通道坐标与跨度。后规划边不得与已占用通道同坐标且跨度重叠，否则两条边画在同一条线上。评估分两轮：**先严格**（节点 + 边间避让都满足），全部被拒时**放宽**（节点避让优先于边间避让）。
- **正交主干**（阶段 0.6）：`orthogonal_route` 用 `used_trunks: Vec<(trunk, s0, s1)>` 登记已占用正交主干；候选主干按 `(节点冲突数, 是否占用主干, |偏移|)` 字典序评分，节点避让优先于边间避让，`trunk` 不穿节点时才登记占用。

### 12.8 几何工具

- **`rounded_polyline`**：拐点用二次贝塞尔升阶为三次平滑过渡，`radius` 不超过相邻段一半，用于通道绕行路径。
- **`points_to_route`**：折线点序列 → 路由段，跳过重复点、合并共线段。
- **`sample_path`**：贝塞尔按 n 等分采样为折线，供碰撞检测。

> **阶段 0 未完成项与本节的对应**：0.5（布局常量收敛）应随阶段 4 一起处理；0.3/0.4/0.6 均已完成，见 §3。

---

## 13. 阶段 5 —— 官方 golden 对齐收敛

> 依据：`docs/svg_alignment_report.md`（几何视角，逐图解析，重判自动 diff 误报）
> 与 `docs/svg_alignment_report-qwen.md`（视觉细节视角，画布/尺寸/样式）。
> 两份报告的结论在此合并为一份可执行清单，按新管线分层落地。

### 13.0 方法：以官方 golden 为唯一基准

所有对齐常量**实测自 `tests/golden/golden/*.svg`**（mermaid-cli 产物），不靠记忆或文档推测。
典型推导（flowchart 矩形节点）：

| 用例 | 文本盒 (fo) | 官方节点包围盒 | 推出 |
|---|---|---|---|
| `flowchart__chain` A=Start | 33.80 × 24 | 93.80 × 54 | W = text_w + 60，H = text_h + 30 |
| `flowchart__dense` A | 10.67 × 24 | 70.67 × 54 | 同上，**无最小宽度** |
| `flowchart__shapes` Circle | 40.89 × 24 | r = 27.95 | 直径 = max(text_w, text_h) + 15 |
| `flowchart__shapes` Decision | 61.36 × 24 | 115.36 × 115.36 | 正方形菱形，边长 = max(w,h) + 54 |
| `state__basic` Idle | 25.8 × 24 | 41.80 × 40 | state padding = 8（与 flowchart 的 30/15 **不同**） |
| `flowchart__chain` 层心 y | — | 35 / 139 / 243 | 层距 104 = 节点高 54 + `rankSpacing` 50 |

> 关键发现：官方**不同形状的高度并不统一**（rect 54、parallelogram/trapezoid 39、stadium 39），
> 且 flowchart 与 state 的圆角矩形 padding 不同。故 padding 必须做成**形状/图表相关的表**，
> 不能是一个全局常量。

### 13.1 视觉常量对齐（theme + paint + materialize）✅

| 项 | 改前 | 官方 golden | 落点 |
|---|---|---|---|
| 边箭头 | `<polygon fill="none">`（空心） | `.arrowheadPath{fill:#333333}` 实心闭合三角 | `paint/mod.rs::arrow_element`：`EdgeEnds::Arrow`/`TriangleFilled` → 实心；`Triangle`（class 继承）保持空心 |
| 连线宽度 | 1.5px | `.edge-thickness-normal{stroke-width:1px}` | `theme::EDGE_WIDTH = 1.0`；新增 `EDGE_WIDTH_THICK = 3.5`（`.edge-thickness-thick`） |
| sequence 参与者盒 | `#ECECFF` / `#9370DB` / rx 5 | `<rect fill="#eaeaea" stroke="#666" rx="3">` | `theme::sequence::{ACTOR_FILL, ACTOR_STROKE, ACTOR_RADIUS}` |
| sequence 生命线 | 虚线 6,4 / 1px | `<line stroke-width="0.5px" stroke="#999">` **细实线** | `theme::sequence::LIFELINE_WIDTH = 0.5` + 去掉 dash |
| sequence 消息线 | 1.5px / dash 3,4 | `stroke-width="2"`、`dasharray: 3, 3` | `theme::sequence::{MESSAGE_WIDTH, MESSAGE_DASH}` |
| sequence 备注框描边 | `#9370DB` | `#666` | `theme::sequence::NOTE_STROKE` |
| state start/end 尺寸 | 32×32 / 36×36 | `<circle class="state-start" r="7">`（14×14） | `extract/state.rs::{START_SIZE, END_SIZE}` |

### 13.2 节点尺寸自适应（measure）✅

**问题**：旧实现强制 `NODE_MIN_W=120 / NODE_MIN_H=60`、`NODE_PAD_X=22 / NODE_PAD_Y=12`，
节点宽度与文本无关（`Start` 画成 120×60，官方 93.8×54），是两份报告都点名的 P0 项。

**改动**：
1. `theme`：`NODE_PAD_X=30 / NODE_PAD_Y=15`（实测值），`NODE_MIN_W/H` 降为 16（仅防退化，不干预排版）。
2. `measure::shape_padding(shape) -> (pad_x, pad_y, square)`：形状 → padding 表（实测自 golden），
   `square=true` 强制正方形（圆 / 双圆 / 菱形）；圆柱额外按 `/0.8` 加上下椭圆弧高度。
3. **`line-height: 1.5`**：官方 `<foreignObject>` 高度 = `font-size × 1.5`（16 → 24），
   而 lievisual 默认返回字体固有行高 ≈18.6，导致节点矮 ~5px。
   新增 `theme::text_style(color, size, align, baseline)` 统一注入行高，
   `measure` 与 `materialize` **必须共用**（否则文本垂直位置会偏）。
4. **同形状不同 padding**：state 与 flowchart 同用 `ShapeKind::Rounded` 但 padding 不同。
   新增 `SizeHint::Padded { pad_x, pad_y }`，`extract/state.rs` 用 `Padded{8,8}` 覆盖形状默认值。

> 验证：`flowchart__chain` 节点由 120×60 → 95.01×54（官方 93.80×54，宽度残差 1.2px 来自字体度量）；
> `state__basic` 由 120×60 → 42.81×40（官方 41.80×40）。

### 13.3 布局间距对齐（engine）✅

- `LAYER_GAP` 56 → **50**、`NODE_GAP` 48 → **50**（官方 `rankSpacing` / `nodeSpacing`）。
- sequence（官方 `sequence__activation`：列心 75/275/475，行 y 111/157/201/247）：
  `SEQ_BOX_MIN_W` 80 → **150**（`minActorWidth`）、`SEQ_BOX_H` 40 → **65**、
  `SEQ_COL_GAP` 140 → **50**（`actorMargin`）、`SEQ_MSG_SPACING` 50 → **46**、`SEQ_ROW_GAP` 30 → **46**。

### 13.4 pie 图例 + 外圈（materialize::emit_pie）✅

官方结构（实测 `pie__basic` / `pie__showdata`）：
```
<circle r="186" class="pieOuterCircle"/>            ← 外圈，黑描边 2px
<path r=185 …/> ×N                                   ← 扇区，白描边 2px
<text class="slice">40%</text>                       ← 扇区标签只放**整数百分比**，位于 0.75r
<g class="legend" transform="translate(216, y)">     ← 图例在右侧：18×18 色块 + 名称
```
改动：半径 150 → 185、新增外圈、标签由 `Rust (40.0%)` 改为 `40%`、新增图例
（`showData` 时名称后附 `[数值]`）；标题 y 由 `-r-60` 改为官方的 `-200`。

### 13.5 文本格式对齐（extract）✅

- **class 成员**：`+ String name` → `+String name`（官方可见性符号与类型间**无空格**）。
  实测 golden：`+String name` / `+int age` / `+eat()` / `+get() : String`。
- **sequence 分组块标签**：`loop [retry]` 拆为**两段独立文本** `loop` + `[retry]`
  （官方就是两个 `<text>`，此前合并导致与 golden 的文本集合不一致）。

### 13.6 sequence 激活条（`->>+` / `-->>-`）✅

两份报告均列为缺失功能（报告 B P0#3）。跨层实现：

1. **IR**（`unigraph.rs`）：`SequenceRow::Activation { actor, on }`，紧跟所属 `Message` 行
   —— 激活是**行级事件**（与备注/分组块同类），而非边属性，避免污染通用 `UGEdge`。
2. **IR**（`geograph.rs`）：`Geograph.activations: Vec<GGActivation { actor, x, y0, y1 }>`，纯几何。
3. **extract**：AST 已有 `Message::activation`；`+` 激活**目标**，`-` 取消激活**源**。
   > 语义坑：最初实现为"- 取消激活目标"，与官方 golden 对照后修正为**源**
   >（`Server-->>-Client` 结束的是 Server 的激活条）。
4. **engine**：`sequence_geometry` 在遍历语句行时维护 `open_act: HashMap<actor, Vec<f64>>` 栈
   （支持嵌套激活）；`Activation` 行不占行高，起点取前一行的 y；未显式取消的激活延伸到内容底部。
5. **materialize**：`<rect width="10" fill="#EDF2AE" stroke="#666">`，z=0（在消息线之下）。

> 验证：`sequence__activation` 的激活条由「无」→ `x=270 y=111 h=138`（官方 270/111/136）、
> `x=470 y=157 h=46`（官方 470/159/42）。

### 13.7 已知偏差（未修，记录备查）

| 项 | 现状 | 原因 / 取舍 |
|---|---|---|
| 画布尺寸 | 仍为「固定 900×700 + `fit_to_canvas` 居中缩放」 | 改 `viewBox` 需 lievisual 暴露接口或后处理 SVG 串；内容居中后空白较多但不影响语义。留待后端支持后处理 |
| `state__fork_join` 的 `join_state` 标签 | 画成无标签横条 | 官方行为依赖**源码顺序**：`fork_state` 先声明后使用 → 横条；`join_state` 先被转移引用后声明 → 退化为普通带标签状态框。当前 AST 把 `states` / `transitions` 分成两个列表，丢失交错顺序；要对齐需 parser 保留语句序 |
| timeline 结构 | 「横轴 + 年份列 + 事件块」 | 官方为「纵向分段 + 横向事件线」，是 Linear 家族的整体重排，非常量级改动 |
| er 边分段 | crow's-foot 拆成多条 `path` | 报告已判定为**表达差异而非布局错位**，暂不处理 |
| state start/end 画法 | `ellipse` | 官方 `circle` + `path`；纯画法差异，几何已对齐 |

### 13.8 阶段 5 的回归保障

- `cargo test --workspace` **0 failed**（含 `official_compare_test` 的结构回归门槛）。
- 新增/更新断言：
  - `pipeline_test::sequence_renders_activation_bars`（激活条颜色 / 宽度 / 跨行跨度）
  - `pipeline_test::pie_diagram_*`（整数百分比、外圈、18×18 图例色块）
  - `pipeline_test::sequence_renders_loop_block_label`（拆分的 `loop` + `[retry]`）
  - `extract/sequence::activations_emit_rows_after_their_message`（行序 + 激活对象语义）
  - `extract/sequence::note_carries_text_targets_and_placement`
  - `extract/class`（成员格式 `#int age`）
- 修正 1 处**尺度相关**的旧断言：`edge_avoidance_test::channel_routes_do_not_overlap`
  原判据写死 `x < -60 / x > 60`（按旧 120px 节点宽标定），节点宽度自适应后失效；
  改为相对布局中轴 `x < 0 / x > 0`，表达同一意图（两条回边通道分居左右）且与节点尺寸无关。
